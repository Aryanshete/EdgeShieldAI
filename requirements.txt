# Core runtime dependencies for the EdgeShield MVP.
# Install a ROCm-specific PyTorch build separately when deploying on supported
# AMD infrastructure; see docs/amd-rocm.md for ROCm acceleration setup.
torch
torchvision
ultralytics
# ByteTrack's linear-assignment solver (lapx provides binary wheels for cloud)
lapx>=0.5.5
lap>=0.5.12
opencv-python
numpy
streamlit
pandas
pillow
pyyaml
onnxruntime

# Test runner used by the detector unit tests.
pytest
