# Models

Model weights are intentionally not committed to this repository. Ultralytics
downloads the selected YOLO model on first use. Record the exact model variant
and runtime device in a reproducible deployment configuration.
